<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobilebank">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UAS_F_PBP_Kelompok_6">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".home"></activity>
        <activity android:name=".informasi"></activity>
        <activity android:name=".transfer"></activity>
        <activity android:name=".validasi"></activity>
        <activity android:name=".laporan"></activity>
        <activity android:name=".register"></activity>
        <activity android:name=".isisaldo"></activity>
    </application>

</manifest>